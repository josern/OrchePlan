# Database Configuration - API and DB on same server
# Local PostgreSQL connection (same server)
DATABASE_URL=postgresql://orcheplan_user:secure_password@localhost:5432/orcheplan_db?sslmode=prefer
DIRECT_URL=postgresql://orcheplan_user:secure_password@localhost:5432/orcheplan_db?sslmode=prefer

# SSL Options for same-server setup:
# sslmode=disable    - No encryption (faster, but only safe on localhost)
# sslmode=prefer     - Try SSL, fallback to plain (recommended for same server)
# sslmode=require    - Force SSL (use if you want encryption even locally)

# Alternative configurations:
# No SSL (fastest for localhost):
# DATABASE_URL=postgresql://orcheplan_user:secure_password@localhost:5432/orcheplan_db

# Force SSL (most secure):
# DATABASE_URL=postgresql://orcheplan_user:secure_password@localhost:5432/orcheplan_db?sslmode=require

# Unix socket connection (most secure for same server):
# DATABASE_URL=postgresql://orcheplan_user:secure_password@/orcheplan_db?host=/var/run/postgresql

JWT_SECRET=replace_this_with_a_real_secret

# SSL Certificate paths (only needed if using sslmode=verify-ca or verify-full)
# SSL_CERT_PATH=/etc/ssl/certs/client-cert.pem
# SSL_KEY_PATH=/etc/ssl/private/client-key.pem  
# SSL_CA_PATH=/etc/ssl/certs/ca-cert.pem