DATABASE_URL=postgresql://postgres:postgres@localhost:5432/orcheplan_dev

# If using docker-compose, the app container uses:
# DATABASE_URL=postgresql://postgres:postgres@db:5432/orcheplan_dev
JWT_SECRET=replace_this_with_a_real_secret